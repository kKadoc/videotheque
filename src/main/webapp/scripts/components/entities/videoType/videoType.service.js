'use strict';

angular.module('videothequeApp')
    .factory('VideoType', function ($resource) {
        return $resource('api/videoTypes/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
