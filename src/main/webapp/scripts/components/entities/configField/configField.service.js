'use strict';

angular.module('videothequeApp')
    .factory('ConfigField', function ($resource) {
        return $resource('api/configFields/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
